<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xiamu.publisher.mapper.PageHelperMapper">
    <select id="selectManySelective" resultType="com.xiamu.publisher.bean.ResoinseEntityDto"  >
        SELECT  pagenum pageNum,pagesize pageSize,dt dtTime FROM pagehelper group by pagenum,pagesize,dt
    </select>

</mapper>
